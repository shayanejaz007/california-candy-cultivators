# Accessibility

The design includes keyboard-operable buttons, labels, dialog roles, Escape-to-close behavior, high-contrast status colors, semantic headings and reduced-motion support.

Before launch, run a keyboard-only pass and Lighthouse/axe scan after real media and copy are added.
