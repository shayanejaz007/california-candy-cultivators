# Frontend

Public components live in `app/components`. Key components are `Hero`, `SiteBody`, `StrainCard`, `InquiryModal`, `AgeGate`, `Nav`, and `Footer`.

Dynamic strain pages use `app/strains/[slug]`. Media renders the selected cover first and falls back to design placeholders when no asset exists.
