# Design system

The supplied dark editorial visual system is centralized mostly in `lib/constants.js`: colors, typography stacks, status colors and media paths. Global resets and motion/accessibility behavior are in `app/globals.css`.

Keep status colors semantically consistent: Available, Low Stock, Coming Soon and Sold Out must not rely on color alone.
