# SEO

Global metadata is defined in `app/layout.js`; dynamic strain metadata is generated in `app/strains/[slug]/page.js`. The project includes sitemap and robots routes, canonical URLs and Open Graph data.

Replace `NEXT_PUBLIC_SITE_URL` before launch and add real social preview imagery when final media is available.
